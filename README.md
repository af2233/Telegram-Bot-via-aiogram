# Telegram-Bot-via-aiogram

Телеграм-бот со встроенной нейросетью. Для запуска потребуется ollama и скачанная текстовая модель. Создайте файл .env с переменными TOKEN и LLM_MODEL.

Запуск:
```
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
python bot.py
```
